import React from 'react'
import { shallow } from 'enzyme'

import Projector from '../Projector'

describe('Projector render...', () => {
  it('renders without error', () => {
    shallow(<Projector />)
  })
})

describe('Projector pending...', () => {
  it('needs more tests', () => {
    // It does...
  })
})
